chmod
	recursivo para subpastas
		chmod -R 777 .
	para em um arquivo
		chmod 777 [file_name]

add user to sudo
	usermod -aG sudo [username]

open terminal folder path onto explorer
	nautilus .

show file 
	cat <file-name>

#https://www.edivaldobrito.com.br/openjdk-8-no-ubuntu-19-04/

install java

sudo apt-get update
sudo apt search jdk-17
sudo apt-get install openjdk-17-source

#https://askubuntu.com/questions/84483/how-to-completely-uninstall-java

sudo dpkg --list | grep -i jdk
sudo apt-get purge openjdk*
sudo apt-get purge icedtea-* openjdk-*
sudo dpkg --list | grep -i jdk


delete folder not empty recusivo
rm -rf lampp

nautilus .

